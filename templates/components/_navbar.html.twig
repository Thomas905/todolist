<nav class="navbar navbar-expand-lg navbar-light bg-light">
    <div class="container-fluid">
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <a class="navbar-brand mt-2 mt-lg-0" href="{{ path('task_index') }}">
                <img src="{{ asset('build/images/logo.png' ) }}" alt="">
            </a>
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                <li class="nav-item">
                    <a class="nav-link" href="{{ path('task_new') }}">Ajouter une tâche</a>
                </li>
            </ul>
        </div>
        <div class="d-flex align-items-center justify-content-center">
            <h3 class="text-reset h6">{{ user.email }} <a href="{{ path('auth_logout') }}" class="text-black ms-5"><i class="fa-solid fa-right-from-bracket"></i></a></h3>

        </div>
    </div>
</nav>